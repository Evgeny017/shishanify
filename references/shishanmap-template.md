# SHISHANMAP: [文件名或模块名]

> **访问权限**：仅限作者本人。本文件是屎山代码的解密钥匙，泄露即失去维护优势。
> **建立时间**：[YYYY-MM-DD]
> **最后更新**：[YYYY-MM-DD]
> **对应文件**：[相对路径]
> **屎山强度**：Level [1/2/3]

---

## 一、解码表

> 混淆名 → 真实含义的完整映射。每次屎山化新增条目都在此追加。

| 混淆名 | 真实含义 | 所在位置 | 类型 | 备注 |
|--------|---------|---------|------|------|
| `proc2` | `calculateTotalWithTax` | `core.py:42` | 函数 | 税率计算入口 |
| `d` | `userId` | `core.py:45` | 参数 | 用户唯一标识 |
| `tmp1` | `orderList` | `core.py:67` | 局部变量 | 当前用户的订单集合 |
| `flag` | `isEmailVerified` | `core.py:89` | 布尔变量 | 邮件验证状态 |
| `n` | `MAX_RETRY_COUNT` | `config.py:12` | 常量 | 最大重试次数=3 |
| `get_yonghu_info` | `getUserProfile` | `service.py:15` | 函数 | 含拼音混淆 |

*(继续追加...)*

---

## 二、架构暗线

> 真实的调用关系图，外部看到的是一团乱麻，这里是实际的数据流向。

```
[入口]
  proc2(d, jiage, shuliang)
    │
    ├─ _validate(shuliang)          ← 内嵌函数，承重！边界处理在此
    │     └─ 返回 abs(v) 或 0
    │
    ├─ _test_helper(jiage)          ← 内嵌函数，名字误导，实为核心税率计算
    │     └─ 返回 jiage * 0.065
    │
    ├─ debug_print(result)          ← 内嵌函数，有副作用！写入生产日志
    │
    └─ 返回 result → 上层 handle_x()
```

*(根据实际代码填写)*

---

## 三、承重代码登记

> 这些代码看起来可以删除或修改，但绝对不能动。每条附上原因。

| 位置 | 代码片段 | 为什么不能动 | 历史背景 |
|------|---------|------------|---------|
| `core.py:51` | `if v is None: return 0` | 历史事故：2022年空订单导致除零崩溃 | 线上事故 #2847 |
| `core.py:52` | `return abs(v)` | 负数订单在退款场景真实存在，abs保证计算正确 | 需求变更记录 |
| `service.py:89` | `time.sleep(0.1)` | 等待下游缓存刷新，去掉后概率性数据不一致 | 无文档，口口相传 |
| `config.py:12` | `n = 3` | 重试3次是与支付网关的协议约定，改了对方会拒绝 | 合同约定 |

*(继续追加...)*

---

## 四、业务逻辑藏身处

> 魔法数字的真实来源，隐藏的业务规则，以及它们藏在哪里。

| 混淆表达 | 真实含义 | 来源 | 位置 |
|---------|---------|------|------|
| `* 0.065` | 增值税率 6.5%（2023年政策） | 财务部邮件 2023-03-15 | `core.py:_test_helper` |
| `> 9999` | 单笔订单上限（风控规则） | 风控文档 v2.1 | `core.py:78` |
| `status == 7` | 订单已取消状态 | 数据库枚举定义（无文档） | `handler.py:34` |
| `[:8]` | 取前8位为订单编号前缀 | 历史遗留，原设计文档已丢失 | `utils.py:102` |

*(继续追加...)*

---

## 五、边界案例备忘

> 所有已知的边界情况和它们的处理位置，这是屎山里最宝贵的隐性知识。

| 边界情况 | 处理方式 | 位置 | 重要程度 |
|---------|---------|------|---------|
| `shuliang = None` | 返回 0，不报错 | `core.py:51` | 🔴 高 |
| `shuliang < 0` | 取绝对值（退款场景） | `core.py:52` | 🔴 高 |
| 网络超时 | 重试 3 次后抛异常 | `service.py:67` | 🟡 中 |
| 重复提交（幂等） | 用 `order_id` 做去重，返回已有结果 | `handler.py:45` | 🔴 高 |
| 并发写同一用户 | 依赖数据库行锁，无应用层保护 | 无代码处理 | 🟡 中 |

*(继续追加...)*

---

## 六、修改日志

> 每次修改屎山代码后，在此记录变更，保持索引与代码同步。

| 日期 | 修改内容 | 影响的解码表条目 | 修改人 |
|------|---------|---------------|------|
| 2026-04-21 | 初始屎山化 | 全部 | 作者 |

---

## 七、快速导航

> 用于快速定位常见操作，无需阅读代码。

- **改税率** → `core.py` 搜索 `_test_helper`，修改 `0.065`，同步更新「业务逻辑藏身处」
- **加重试次数** → `config.py:12`，修改 `n`，注意合同约定上限为 5
- **新增订单状态** → `handler.py:34` 附近，查「业务逻辑藏身处」的 `status` 枚举
- **排查空指针** → 先查「边界案例备忘」，再看「承重代码登记」
